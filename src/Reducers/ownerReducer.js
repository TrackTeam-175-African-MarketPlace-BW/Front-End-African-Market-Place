const initialState = {
  id: "",
  name: "",
  email: "",
  error: "",
  isLoading: false,
  isEditing: false,
  itemsForSale: [],
};

export const ownerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
